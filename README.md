# New immediate tab Chrome extension

Chrome extension which provides a shortcut to open a tab immediately after the active tab, instead of at the end of the tab list.

Default Mac shortcut is `Command+Ctrl+T`.

